{
  "name": "hte-app",
  "version": "1.0.0",
  "description": "High-Throughput Experimentation (HTE) Design Application",
  "main": "backend/app.py",
  "scripts": {
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-backend": "cd backend && python app.py",
    "start-frontend": "cd frontend && npm start",
    "install-all": "npm install --legacy-peer-deps && cd frontend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build",
    "dev": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "audit": "npm audit && cd frontend && npm audit",
    "audit-fix": "npm audit fix && cd frontend && npm audit fix",
    "clean": "rm -rf node_modules frontend/node_modules package-lock.json frontend/package-lock.json && npm install --legacy-peer-deps && cd frontend && npm install --legacy-peer-deps",
    "clean:win": "if exist node_modules rmdir /s /q node_modules && if exist frontend\\node_modules rmdir /s /q frontend\\node_modules && if exist package-lock.json del package-lock.json && if exist frontend\\package-lock.json del frontend\\package-lock.json && npm install --legacy-peer-deps && cd frontend && npm install --legacy-peer-deps && cd ..",
    "postinstall": "cd frontend && npm install --legacy-peer-deps"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "hte",
    "chemistry",
    "experiment",
    "react",
    "flask"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
